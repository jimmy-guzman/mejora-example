// This file intentionally contains issues for mejora demonstration

// ESLint issue: unused variable
const unusedVariable = 'This variable is never used';

// TypeScript issue: implicit any type
function greet(name) {
  console.log(`Hello, ${name}!`);
}

greet('mejora');
